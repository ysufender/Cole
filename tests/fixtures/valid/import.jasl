import std::io::console as Console;

let main = fn (args: [][]u8) -> i32 {
    let test = #(@noComptime) 12;

    {
        let idx: mut u32 = 0;
        while idx < args.len {
            Console::println(args[idx]);
            idx = idx + 1;
        }
    }
};

import std::math;

let Some = struct {
    pub let Other = struct {
        pub let Mother = struct {
            pub let def: u32 = 0;
        };
    };
};

let main = fn () -> i32 {
    let b = Some::Other::Mother::def;
    return 0;
};

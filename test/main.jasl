/*
let test = #(@export) fn () -> void {
};

let main = fn () -> i32 {
    let Some = struct {
        test: mut i32
    };

    let c: mut Some = Some(0);
    defer c = Some(1);

    return c.test;
};
*/

let Some = struct {
    test: mut i32
};

let main = fn () -> i32 {
    let cello: mut Some = Some(1);
    cello = Some(2);
    return 1;
};
